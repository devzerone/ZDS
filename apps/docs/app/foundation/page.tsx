export default function FoundationPage() {
  return (
    <section className="docs-panel">
      <span className="docs-eyebrow">Foundation</span>
      <h1>공통 토큰과 디자인 언어의 기초를 확인합니다.</h1>
      <p>
        Foundation guidance explains token structure, naming rules, and theme interpretation before
        component-level usage. 컴포넌트별 스타일 결정을 보기 전에 semantic token 레이어를 먼저 이해하는
        흐름을 권장합니다.
      </p>
    </section>
  );
}
