{
  "name": "@zds/kotlin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prebuild": "node ../tokens/scripts/build-platform-tokens.mjs --platform kotlin --sync-consumer-root .",
    "build": "gradle --no-daemon -p . build",
    "validate:tokens-consumption": "node ../tokens/scripts/build-platform-tokens.mjs --platform kotlin --sync-consumer-root . --validate-consumer"
  }
}
